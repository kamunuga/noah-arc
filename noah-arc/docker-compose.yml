version: '3.9'
services:
  noah-arc:
    build: .
    ports:
      - '5000:5000'
    environment:
      - PORT=5000
      - JWT_SECRET=replace-this-with-a-long-random-string
    volumes:
      - ./server/data:/usr/src/app/server/data
      - ./server/public:/usr/src/app/server/public
